apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose-deploy.yml -o ./k8s
    kompose.service.type: ClusterIP
    kompose.version: 1.36.0 (HEAD)
  labels:
    io.kompose.service: codeeval
  name: codeeval
spec:
  ports:
    - name: "3002"
      port: 3002
      targetPort: 3002
  selector:
    io.kompose.service: codeeval
  type: ClusterIP
